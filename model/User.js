const express = require('express')
const router =  express.Router()

// this is for the user to add likes according
// the route will have a username and it will add the user according to their username

router.get('/',(req,res)=>{

}
module.exports = router
